import React, { Component } from "react";

export default class Subheading extends Component {
  render() {
    return (
      <div className="text-center">Lista de tareas pendientes usando React</div>
    );
  }
}
